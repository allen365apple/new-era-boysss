<div class="site-banner onload-animation" aria-hidden="true">
	<span class="banner-haze"></span>
</div>

<style>
	.site-banner {
		position: relative;
		height: 300px;
		margin-top: -72px;
		overflow: hidden;
		isolation: isolate;
		border-bottom: 1px solid var(--border);
		background: var(--banner-background);
	}
	.site-banner::before {
		content: '';
		position: absolute;
		inset: -18%;
		background-image:
			radial-gradient(circle, var(--banner-star-bright) 0 1.4px, transparent 1.8px),
			radial-gradient(circle, var(--banner-star) 0 1px, transparent 1.45px),
			radial-gradient(circle, var(--banner-star-muted) 0 0.8px, transparent 1.2px),
			radial-gradient(circle, var(--banner-star-muted) 0 0.65px, transparent 1px);
		background-position: 24px 18px, 83px 61px, 38px 107px, 119px 35px;
		background-size: 238px 176px, 151px 127px, 103px 149px, 191px 113px;
		opacity: 0.9;
		animation: star-drift 30s ease-in-out infinite alternate;
	}
	.site-banner::after {
		content: '';
		position: absolute;
		inset: 0;
		background-image:
			radial-gradient(circle, var(--banner-star-bright) 0 2px, transparent 2.6px),
			radial-gradient(circle, var(--banner-star) 0 1.2px, transparent 1.8px);
		background-position: 8% 28%, 77% 64%;
		background-size: 420px 260px, 330px 220px;
		opacity: 0.65;
	}
	.banner-haze {
		position: absolute;
		inset: 18% -10%;
		background: var(--banner-haze);
		filter: blur(24px);
		transform: rotate(-3deg);
	}
	@keyframes star-drift {
		to { transform: translate3d(24px, -8px, 0); }
	}
	@media (max-width: 640px) {
		.site-banner { height: 240px; }
	}
	@media (prefers-reduced-motion: reduce) {
		.site-banner::before { animation: none; }
	}
</style>
