<div class="site-banner onload-animation" aria-hidden="true">
	<span class="banner-haze"></span>
	<span class="banner-orbit orbit-a"></span>
	<span class="banner-orbit orbit-b"></span>
</div>

<style>
	.site-banner {
		position: relative;
		height: 220px;
		margin-top: -72px;
		overflow: hidden;
		isolation: isolate;
		border-bottom: 1px solid var(--border);
		background: var(--banner-background);
	}
	.site-banner::before {
		content: '';
		position: absolute;
		inset: -25%;
		background-image:
			radial-gradient(circle, var(--banner-star) 0 1px, transparent 1.4px),
			radial-gradient(circle, var(--banner-star-muted) 0 1px, transparent 1.5px),
			radial-gradient(circle, var(--banner-star-muted) 0 0.8px, transparent 1.2px);
		background-position: 18px 28px, 92px 74px, 42px 118px;
		background-size: 196px 142px, 274px 196px, 152px 214px;
		opacity: 0.7;
		animation: star-drift 24s ease-in-out infinite alternate;
	}
	.banner-haze {
		position: absolute;
		inset: 18% -10%;
		background: var(--banner-haze);
		filter: blur(24px);
		transform: rotate(-3deg);
	}
	.banner-orbit {
		position: absolute;
		left: 50%;
		top: 50%;
		width: min(58vw, 760px);
		aspect-ratio: 1;
		border: 1px solid var(--border);
		border-radius: 50%;
		transform: translate(-50%, -50%) scaleY(0.16) rotate(-4deg);
		opacity: 0.55;
	}
	.orbit-b {
		width: min(78vw, 1050px);
		transform: translate(-50%, -50%) scaleY(0.12) rotate(5deg);
		opacity: 0.32;
	}
	@keyframes star-drift {
		to { transform: translate3d(24px, -8px, 0); }
	}
	@media (max-width: 640px) {
		.site-banner { height: 172px; }
	}
	@media (prefers-reduced-motion: reduce) {
		.site-banner::before { animation: none; }
	}
</style>
