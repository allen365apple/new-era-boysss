# Podcast 自動化與審稿

這份文件說明「RSS → 本機逐字稿 → 文章 → 自動檢查 → 直接發布 → 後台修正」的現行流程。文章寫作仍以 `部落格改寫規則.md` 為唯一正本。

## 共用人名與專有名詞對照表（每次必讀）

Podcast 產線的共用正本是 **`docs/KB2_人名與專有名詞對照表.md`**。這份表同時適用於「音檔 → SRT」與「SRT → 部落格文章」，每次排程執行都必須完整讀取，不能只記得四位主持人的正確寫法。

- 音檔轉 SRT 前，將 KB2 的正確人名與專有名詞放進辨識提示；產生 SRT 後，再依 KB2 做安全的錯字正規化。
- SRT 改寫文章前，再完整讀一次 KB2；文章中的主持人、來賓、作品名、節目名與概念詞都要逐一對照。
- `博恩`、`博士` 等表內標示的陷阱不能無腦替換；發音或上下文不確定時，保留疑點並列入人工確認清單。
- KB2 缺失、讀取失敗或必要名稱不完整時，停止該集流程，不得產生或發布文章。

## 自動化會做什麼

1. 讀取 SoundOn RSS，以集數與單集 GUID 判斷目前節目清單。
2. 新集數與歷史補文使用不同佇列，避免彼此重複處理。
3. 從 RSS 的 `enclosure` 下載 MP3 到本機。
4. **完整讀取 `docs/KB2_人名與專有名詞對照表.md`**，把正確名稱與常見辨識錯字帶進辨識提示；再使用本機 `whisper.cpp`，優先沿用 Memo AI 已下載的 `Small` 模型轉成 SRT。EP2 已用 Large 轉完，保留既有結果、不重跑。
5. `podcast:prepare` 成功後**不得停在素材階段**：立即讀取該集 metadata 與 SRT，依 KB2 做人名與專有名詞檢查，再依 `部落格改寫規則.md` 產生含 `---` 分隔線、文末小結、`draft: false` 的 Markdown 文章。前後收聽文案與連結由版型自動顯示。
6. 使用 `speak-human-tw` 的部落格「中」力度做第二輪校稿。自動產線採「跳過確認、事後摘要」，先保護集數、日期、KB2 已確認的專有名詞、公開暱稱、平台連結與已選定引言，再檢查 38 種 AI 痕跡、台灣用語與全形標點；摘要只留在任務紀錄，不寫進文章。
7. 執行文章結構、公開暱稱、議題分類、事實保真與敏感資訊檢查。
8. 同步單集封面與 SoundOn／Apple Podcasts／Spotify 連結。
9. 建置成功後才提交並推送，由 Cloudflare 直接公開；發現錯誤再從後台修改。

音檔、逐字稿、RSS metadata 與處理狀態都放在 repo 上層的本機資料夾，不會提交到公開 GitHub：

```text
podcast-automation/
├── audio/       # RSS 下載的 MP3
├── metadata/    # 單集 RSS 資訊
├── models/      # Whisper 模型
└── state.json   # 啟用自動化時的集數基準

各集逐字稿/
└── EPxx 自動轉錄.srt
```

## 最方便的上線後修正方式

1. 打開 `https://new-era-boysss.pages.dev/admin/`。
2. 進入「文章」，選擇「已發布」篩選。
3. 點文章後直接修改標題、摘要、主持人、分類與內文。
4. 依下方「五分鐘校對」順序檢查。
5. 直接儲存修改；若問題嚴重、需要先下架，開啟最上方的「草稿」再儲存。
6. GitHub 收到變更後，Cloudflare Pages 會自動重新部署。

後台已停用刪除文章，避免審稿時誤刪。排程完成時也會在 Codex 的排程紀錄列出新增文章與需要特別確認的地方。

### 五分鐘校對

- **先看事實**：本集標題、來賓、主持人、作品名與專有名詞是否正確。
- **再看引言**：原話有沒有被改變意思；不確定講者的引言不得署名。
- **快速掃內文**：是否有逐字稿沒有的事件、心理狀態或過度延伸。
- **確認閱讀感**：4–5 節，每節至少兩個正文段落；最後一節正文後有 `---` 分隔線，再接 1–2 段取材自主持稿或 SRT 尾段的小結，且沒有明顯 AI 套話。
- **最後看分類與摘要**：議題分類是否貼切，Google 摘要是否講清楚文章內容。

## 本機指令

在 `site/` 執行：

```bash
npm run podcast:status
npm run podcast:queue -- --mode=backfill --limit=2
npm run podcast:queue -- --mode=new --limit=2
npm run podcast:prepare -- --episode=2
npm run validate:article -- src/content/blog/ep2.md
npm run sync:episode-links -- --write --include-drafts
```

`podcast:prepare` 可重複執行：已有逐字稿時會直接沿用，不會重新下載和轉錄。prepare 成功後必須在同一批次接續「寫文章 → 逐篇驗證 → 同步連結 → 測試／建置 → 敏感資訊檢查 → commit／push」；不得只回報素材已就緒就結束。任何步驟失敗時，都不得建立空文章或把該集標成完成；應修正後從中斷步驟繼續。

## 安全與發布原則

- 自動化通過檢查後以 `draft: false` 直接公開；需要暫時下架時才改為 `true`。
- 逐字稿、音檔與模型不可進入 public repo。
- 公開主持人名稱只能使用柏文、孝成、博志、沁儒。
- 文章提交前必須檢查 Email、電話、地址、API 金鑰、內部 URL 與成員真實姓名。
- 自動檢查只能降低風險；上線後仍可由團隊從後台校對與修正。
- 本機排程需要 Mac 保持開機、連網，並讓 Codex 桌面 App 持續執行。
