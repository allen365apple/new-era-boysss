# 新世紀直男戰士 — 改風格小抄

> 想換顏色、字體、底色?**全部都在一個檔案裡改。**
> 檔案位置:`src/styles/global.css`，最上面的 `:root { ... }` 區塊。
> 改完存檔 → push 到 GitHub → Cloudflare 自動部署 → 全站一起變。

---

## 一、顏色

| 變數 | 現在的值 | 控制什麼 |
|------|----------|----------|
| `--accent` | `#ff75bd`（亮粉紅） | **主色**：文章標題、連結、集數標籤、目錄高亮 |
| `--accent-strong` | `#ff4fa3` | 主要按鈕與高彩度粉紅色 |
| `--accent-dark` | `#ffb5da` | 深色底上的連結 hover 色 |
| `--accent-light` | `#35172b` | 議題標籤與目錄高亮底色 |
| `--accent-pink` | `#ff75bd` | 粉紅點綴、首頁關鍵資訊 |
| `--accent-blue` | `#82aaff` | 首頁宇宙漸層中的電光藍輔色 |
| `--heading-secondary` | `#9baeff` | 二級標題的藍紫色 |
| `--heading-tertiary` | `#ff91c9` | 三級標題的粉紅色 |
| `--title-gradient` | 粉紅至藍紫漸層 | 最大標題與頁面主標題 |
| `--surface` | `#07080b` | **全站墨黑底色** |
| `--surface-soft` | `#11131a` | 卡片、引言與收聽資訊區塊 |
| `--surface-raised` | `#171a23` | 首頁主視覺與較高層級區塊 |
| `--border` | 半透明骨白 | 細分隔線 |
| `--text-primary` | `#f4f0e8` | 主要標題與高對比文字 |
| `--text-secondary` | `#c7c5cc` | 內文文字 |
| `--text-muted` | `#92929e` | 提示文字與日期 |
| `--card-bg` | `#171a23` | Fuwari 導覽列與互動卡片底色 |
| `--btn-plain-bg-hover` | `#232737` | 導覽列與按鈕指向時展開的底色 |
| `--page-background` | 深色宇宙漸層 | 全站頁面背景；淺色模式有獨立版本 |
| `--top-starfield-*` | 多層不規則星點與漸淡遮罩 | 頁面頂端星空，向下逐漸淡出，不是獨立 Banner |
| `--star-*` | 半透明白、藍、粉紅 | 星點亮度與光暈；調整時避免干擾內文閱讀 |
| `--banner-background` | 舊 Banner 相容變數 | 目前公開頁面不渲染獨立 Banner，除非日後重新啟用 `SiteBanner` |

淺色模式的值集中在同一檔案接續的 `:root[data-theme='light']`。如果調整品牌色，請同步檢查深色與淺色兩組值，避免其中一種模式的文字對比不足。

### 範例

```css
/* 想把整站主色改成綠色：只改這一行 */
--accent: #1f9d5c;

/* 想把墨黑底改成純白：需要同步調整三組文字色，避免對比不足 */
--surface: #ffffff;
```

改 `--accent` 一行，首頁標題、文章標題、所有標籤、連結、目錄高亮會**同時**變色。

---

## 二、字體

| 變數 | 控制什麼 |
|------|----------|
| `--font-sans` | 全站字體。目前是思源黑體 `"Noto Sans TC"` |

換字體只改這一行。第一個字型是主要字型，後面是備用（找不到時往後用）。

---

## 三、版面尺寸

| 變數 | 現在的值 | 控制什麼 |
|------|----------|----------|
| `--content-width` | `680px` | 文章內文的寬度（想要更寬的閱讀區就調大） |
| `--radius` | `8px` | 一般圓角（按鈕、輸入框） |
| `--radius-lg` | `14px` | 大圓角（卡片、區塊） |
| `--radius-large` | `1rem` | Fuwari 導覽列與大型互動元件的圓角 |

---

## 四、互動與頁面切換

- 導覽列的展開、縮放回饋與初次載入動畫，採用 Fuwari 的 MIT 授權實作；授權資訊在 `THIRD_PARTY_NOTICES.md`。
- 現行頁面不顯示獨立橫幅；宇宙感由 `body` 背景與頂端星空圖層構成，星星往頁面下方淡出。
- 深色／淺色切換與文章搜尋在 `Header.astro`；相關顏色仍由 `global.css` 變數控制。
- 頁面切換由 `@swup/astro` 處理，設定集中在 `astro.config.mjs`。一般改色不必碰這個檔案。
- 動畫用到的顏色仍須回到 `global.css` 最上方的變數調整，不要把色碼寫進元件。
- 系統會遵守裝置的「減少動態效果」設定，請勿移除 `prefers-reduced-motion` 規則。

---

## 找色碼的小工具

不知道想要的顏色色碼?Google 搜尋「**color picker**」會跳出取色盤，
選好顏色複製那個 `#xxxxxx`，貼到對應變數即可。

---

## 重要原則

- **不要去各個元件檔案裡改顏色**，那會改得零零落落。永遠改 `global.css` 的 `:root` 變數。
- 改完想看效果:本機 `npm run dev` 開 `localhost:4321`，或 push 後看線上網站。
- 不確定改哪個 → 直接問維護網站的 AI 助理，貼上你想要的效果。

---

*最後更新：2026-08-16 · 目前採全頁背景＋頂端漸淡星空，不使用獨立 Banner。跨 AI 變更摘要見 `AGENTS.md`。*
